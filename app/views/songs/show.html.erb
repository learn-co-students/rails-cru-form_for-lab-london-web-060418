<h1><%= @song.name %> </h1>
<p> <%= link_to @song.genre.name, genre_path(@song.genre) %></p>
<p> <%= link_to @song.artist.name, artist_path(@song.artist) %></p>


<p><%= link_to "Edit this song", edit_song_path(@song) %></p>
<p><%= link_to "Back to songs", songs_path %></p>
