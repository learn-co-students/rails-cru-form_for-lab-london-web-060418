<h1><%= @song.name %></h1>

<h2><%= link_to @song.artist.name, @song.artist %></h2>
<h2><%= link_to @song.genre.name, @song.genre %></h2>

<%= form_tag songs_path, method: 'get' do %>
  <%= submit_tag "Back to Songs" %>
<% end %>
